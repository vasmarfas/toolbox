TEAM_ID=

PRODUCT_NAME=Toolbox
PRODUCT_BUNDLE_IDENTIFIER=com.vasmarfas.toolbox

CURRENT_PROJECT_VERSION=10000
MARKETING_VERSION=1.0.0

// TEAM_ID stays empty here: CI passes DEVELOPMENT_TEAM to xcodebuild from a secret,
// and a local signing team goes into Local.xcconfig, which git does not track
#include? "Local.xcconfig"